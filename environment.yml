name: moral_maze
channels:
  - defaults
  - conda-forge
dependencies:
  - python>=3.10
  - pip
  - pip:
      - pygame>=2.5.0
      - pydantic>=2.0.0
      - pyyaml>=6.0
      - openai>=1.0.0
      - python-dotenv>=1.0.0

